project_title:
  type: str
  default: ""

crate_name:
  default: "{{ project_title|lower|replace(' ', '-') }}"
  when: false

crate_description:
  type: str
  default: "{{ crate_name }}"

repository:
  type: str
  default: ""
